#' First, we enter some values into a variable `x`:

x=c(1,2,3,4,5,7)

#' and then we display what `x` contains:

x

#' Finally, we find the mean and standard deviation of `x`:

mean(x)
sd(x)

#' These are 3.67 and 2.16, to two decimal places.